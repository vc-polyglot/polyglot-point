[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "npm install -g pnpm",
  "npx pnpm install --no-frozen-lockfile"
]

[phases.build]
cmds = [
  "npx pnpm --filter @platform/lexipop-decision run build"
]

[start]
cmd = "node packages/lexipop-decision/dist/index.cjs"