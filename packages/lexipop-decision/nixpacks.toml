[phases.setup]
nixPkgs = ["nodejs_22"]
cmds = ["npm install -g pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile=false"]

[phases.build]
cmds = ["pnpm run build"]

[start]
cmd = "node dist/index.cjs"